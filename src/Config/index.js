import typeBooks from "./typeBook";

const config = {
    typeBooks,
    LOCAL_STORAGE_TOKEN_NAME: "popLibaryToken",
    apiUrl: "https://e-service-bookstore.herokuapp.com/api/v1/",
};

export default config;
